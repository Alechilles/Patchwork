package com.alechilles.patchwork.coordinator;

/** Identifies whether a runtime was supplied by the standalone plugin or an embedding host. */
enum PatchworkRuntimeOrigin {
    STANDALONE,
    EMBEDDED
}
